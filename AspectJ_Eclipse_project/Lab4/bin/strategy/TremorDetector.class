package strategy;

//TremorDetector Class (uses Strategy)
public class TremorDetector {
	private DetectionStrategy strategy;

	public void setStrategy(DetectionStrategy strategy) {
		/* No functionality here */ }

	public void analyzeData() {
		/* No functionality here */ }
}